filters:
  # a random number or custom name
  default:
    # can be DEFAULT, null or custom
    permission: DEFAULT
    # the char that will replace those bad words
    replace-char: '*'
    # enable this if you know regex lol
    is-regex: true
    # the patterns (regex) or words that can be filtered
    words:
      - (?i)f(.)?ck
      - (?i)dumbass|idiot
  vip:
    permission: chat.vip
    replace-char: '*'
    is-regex: false
    words:
      - stupid
      - dumb